<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>org.example</groupId>
	<artifactId>workbench-server</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>workbench-server</name>
	<description>workbench-server</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<!-- MyBatis-Plus 不在 Spring Boot BOM 中，版本需自行指定 -->
		<mybatis-plus.version>3.5.17</mybatis-plus.version>
		<jjwt.version>0.12.7</jjwt.version>
	</properties>
	<dependencies>
		<!--
			Spring Boot 4 中 spring-boot-starter-web 已标记 deprecated（见其 pom 的 description），
			替代品是 spring-boot-starter-webmvc：Spring MVC + 内嵌 Tomcat + Jackson。
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!-- @Valid / @NotBlank 等参数校验 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!--
			持久层。artifactId 因 Spring Boot 大版本而异：
			2.x → mybatis-plus-boot-starter，3.x → mybatis-plus-spring-boot3-starter，4.x → mybatis-plus-spring-boot4-starter。
		-->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-spring-boot4-starter</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>

		<!--
			3.5.9 起 JSqlParser 从主包拆出，分页插件 PaginationInnerInterceptor 依赖它。
			不引这个，分页会在运行时报 NoClassDefFoundError。
		-->
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-jsqlparser</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
			只用 spring-security-crypto 取 BCryptPasswordEncoder，
			不引入完整的 spring-boot-starter-security：那会自带一套过滤器链和默认登录页，
			与项目自己的 JWT 拦截器冲突。密码哈希见 CLAUDE.md「必须加盐哈希」。
		-->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<!-- jjwt-impl 与 jjwt-jackson 只需在运行时出现，编译期不直接引用其类 -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
